function sumDouble(a, b) {
    if (a === b)
        return (a + b) * 2;
    return a + b;

    // Норм. Хотя мне больше нравится такое делать через тернарный оператор,
    // но это больше дело вкуса.
}
